services:
  dashboard:
    build: .
    container_name: scraper-dashboard
    restart: unless-stopped
    ports:
      - "3001"
    environment:
      - MONGODB_URI=${MONGODB_URI}
      - NODE_ENV=production
